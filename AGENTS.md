- Prioritize writing reuseable code which can be referenced or extended. That includes reusing classes and types. That also includes styles.
- When building object hirarchies try to use global stores/variables or use other centralised storage only in some mid level managing objects. Leaf objects should not modify global state. For example - during object composition, the composer should translate state generated by composing elements to the global one.
- Try applying object oriented design methods and patterns where reasonably possible.
- While coding, break problems into clear steps and group related actions together.
- Try using Model View Controller pattern when presenting data with a centralised storage in many places

If the request is unclear:
- Ask clarifying questions instead of guessing.
If you are unsure:
- Say so and propose the best possible approach.

- Don't format any code that is unrelated to your changes. For example, if there is a sequence of valiable iitizlisations and you insert a new variable longer then the others, dont format other assignements to make the equal sign be in the same column. Etc.